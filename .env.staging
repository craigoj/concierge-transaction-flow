# Staging Environment Variables
# This file contains staging-specific configuration
# Similar to production but with different endpoints and keys

NODE_ENV=staging

# Supabase Configuration - Staging
# These should be set via deployment platform
VITE_SUPABASE_URL=${VITE_SUPABASE_URL_STAGING}
VITE_SUPABASE_ANON_KEY=${VITE_SUPABASE_ANON_KEY_STAGING}

# Application Configuration - Staging
VITE_APP_NAME=Concierge Transaction Flow (Staging)
VITE_APP_VERSION=1.0.0
VITE_APP_URL=${VITE_APP_URL_STAGING}

# Staging Feature Flags
VITE_ENABLE_ANALYTICS=true
VITE_ENABLE_DEBUG=true
VITE_ENABLE_SENTRY=true

# Monitoring Configuration
VITE_SENTRY_DSN=${VITE_SENTRY_DSN_STAGING}
VITE_VERCEL_ANALYTICS_ID=${VITE_VERCEL_ANALYTICS_ID_STAGING}

# Email Service Configuration
RESEND_API_KEY=${RESEND_API_KEY_STAGING}

# Security Configuration
JWT_SECRET=${JWT_SECRET_STAGING}
ENCRYPTION_KEY=${ENCRYPTION_KEY_STAGING}